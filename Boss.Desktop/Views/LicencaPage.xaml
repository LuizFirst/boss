<Page x:Class="Boss.Desktop.Views.LicencaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="900"
      Background="Transparent"
      Title="LicencaPage">

    <Grid Margin="40">
        <StackPanel>
            <TextBlock Text="🔑 Sistema de Licença" 
                       FontSize="32" 
                       FontWeight="Bold" 
                       Foreground="{StaticResource PrimaryColor}"
                       HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <!-- Formulário -->
            <Border Background="#1A1A1A" CornerRadius="8" Padding="30" Margin="0,0,0,30">
                <StackPanel>
                    <TextBlock Text="Ativar Nova Licença" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <TextBlock x:Name="txtStatus" 
                               Text="{Binding StatusTexto}" 
                               FontSize="16" 
                               Margin="0,0,0,20"/>

                    <TextBlock Text="Chave de Ativação" Margin="0,0,0,8"/>
                    <TextBox x:Name="txtChave" Height="45" FontSize="15" Padding="15,0" Margin="0,0,0,15"/>

                    <TextBlock Text="Tipo de Licença" Margin="0,0,0,8"/>
                    <ComboBox x:Name="cmbTipo" Height="45" SelectedIndex="0">
                        <ComboBoxItem>Vitalicia</ComboBoxItem>
                        <ComboBoxItem>1 Mês</ComboBoxItem>
                        <ComboBoxItem>3 Meses</ComboBoxItem>
                        <ComboBoxItem>6 Meses</ComboBoxItem>
                        <ComboBoxItem>1 Ano</ComboBoxItem>
                    </ComboBox>

                    <Button Content="ATIVAR LICENÇA" 
                            Height="50" 
                            Margin="0,20,0,0"
                            Background="{StaticResource PrimaryColor}" 
                            Foreground="White" 
                            FontWeight="Bold"
                            Click="BtnAtivar_Click"/>
                </StackPanel>
            </Border>

            <!-- Lista -->
            <TextBlock Text="Licenças Ativas" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
            <ListView x:Name="lvLicencas" 
                      Height="300" 
                      Background="#1A1A1A" 
                      Foreground="White"
                      ItemsSource="{Binding Licencas, Mode=OneWay}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Chave" DisplayMemberBinding="{Binding Chave}" Width="180"/>
                        <GridViewColumn Header="Tipo" DisplayMemberBinding="{Binding Tipo}" Width="100"/>
                        <GridViewColumn Header="Ativação" DisplayMemberBinding="{Binding DataAtivacao, StringFormat=dd/MM/yyyy}" Width="100"/>
                        <GridViewColumn Header="Expira" DisplayMemberBinding="{Binding DataExpiracao, StringFormat=dd/MM/yyyy}" Width="100"/>
                        <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Ativa}" Width="70"/>
                        <GridViewColumn Header="Ações" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="5,0">
                                        <Button Content="➕ +1M" 
                                                Height="30" 
                                                Padding="10,0"
                                                Background="#FF6B6B" 
                                                Foreground="White"
                                                Margin="0,0,5,0"
                                                Click="BtnAdicionarMes_Click"
                                                Tag="{Binding Id}"/>
                                        <Button Content="❌ Deletar" 
                                                Height="30" 
                                                Padding="10,0"
                                                Background="#8B0000" 
                                                Foreground="White"
                                                Click="BtnDeletar_Click"
                                                Tag="{Binding Id}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
