<Page x:Class="Boss.Desktop.Views.ProcedimentosPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1100"
      Background="Transparent"
      Title="ProcedimentosPage">

    <Grid Margin="30">
        <StackPanel>
            <Grid>
                <TextBlock Text="Procedimentos" 
                           FontSize="32" 
                           FontWeight="Bold"
                           Foreground="{StaticResource PrimaryColor}"
                           HorizontalAlignment="Left"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="+ Novo Procedimento" 
                            Height="45"
                            Width="180"
                            Background="{StaticResource PrimaryColor}"
                            Foreground="White"
                            FontWeight="Bold"
                            Click="BtnNovo_Click"/>

                    <Button Content="🔄 Atualizar" 
                            Height="45"
                            Width="120"
                            Margin="10,0,0,0"
                            Background="#333"
                            Foreground="White"
                            Click="BtnAtualizar_Click"/>
                </StackPanel>
            </Grid>

            <TextBox x:Name="txtPesquisa" 
                     Height="45" 
                     Margin="0,20,0,20"
                     FontSize="15"
                     Padding="15,0"
                     Background="#1F1F1F"
                     Foreground="White"
                     BorderBrush="#333"
                     BorderThickness="1"/>

            <DataGrid x:Name="dgProcedimentos" 
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Background="#1A1A1A"
                      Foreground="White"
                      RowBackground="#1F1F1F"
                      AlternatingRowBackground="#252525"
                      RowHeight="45"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      Margin="0,10,0,0">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Marca" Binding="{Binding Marca}" Width="120"/>
                    <DataGridTextColumn Header="Modelo" Binding="{Binding Modelo}" Width="180"/>
                    <DataGridTextColumn Header="Número do Modelo" Binding="{Binding NumeroModelo}" Width="150"/>
                    <DataGridTextColumn Header="Tipo de Bloqueio" Binding="{Binding TipoBloqueio}" Width="160"/>
                    <DataGridTextColumn Header="Autor" Binding="{Binding Autor}" Width="120"/>
                    <DataGridTextColumn Header="Data" Binding="{Binding DataCadastro, StringFormat=dd/MM/yyyy}" Width="110"/>

                    <!-- Coluna de Ações -->
                    <DataGridTemplateColumn Header="Ações" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="✏️" 
                        Width="35" 
                        Height="35" 
                        Margin="2"
                        Click="BtnEditar_Click"
                        ToolTip="Editar"/>
                                    <Button Content="🗑️" 
                        Width="35" 
                        Height="35" 
                        Margin="2"
                        Click="BtnExcluir_Click"
                        ToolTip="Excluir"
                        Background="#E63939"
                        Foreground="White"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Page>