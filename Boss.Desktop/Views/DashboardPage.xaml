<Page x:Class="Boss.Desktop.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1000"
      Background="Transparent"
      Title="DashboardPage">

    <Grid Margin="30">
        <StackPanel>
            <TextBlock Text="Dashboard" 
                       FontSize="36" 
                       FontWeight="Bold"
                       Foreground="{StaticResource PrimaryColor}"/>

            <TextBlock Text="Visão Geral do Sistema" 
                       FontSize="18" 
                       Foreground="{StaticResource TextSecondary}"
                       Margin="0,0,0,30"/>

            <!-- Contadores Principais -->
            <WrapPanel Margin="0,0,0,40">
                <Border Width="220" Height="130" Background="{StaticResource CardBackground}" CornerRadius="10" Margin="0,0,20,20" Padding="20">
                    <StackPanel>
                        <TextBlock Text="247" FontSize="42" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Procedimentos Cadastrados" FontSize="14" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                </Border>

                <Border Width="220" Height="130" Background="{StaticResource CardBackground}" CornerRadius="10" Margin="0,0,20,20" Padding="20">
                    <StackPanel>
                        <TextBlock Text="18" FontSize="42" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Usuários Ativos" FontSize="14" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                </Border>

                <Border Width="220" Height="130" Background="{StaticResource CardBackground}" CornerRadius="10" Margin="0,0,20,20" Padding="20">
                    <StackPanel>
                        <TextBlock Text="9" FontSize="42" FontWeight="Bold" Foreground="#E63939"/>
                        <TextBlock Text="Pedidos Pendentes" FontSize="14" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                </Border>
            </WrapPanel>

            <!-- Contadores por Marca -->
            <TextBlock Text="Procedimentos por Marca" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="White"/>
            <WrapPanel>
                <Border Width="180" Margin="0,0,15,15" Padding="15" Background="#1F1F1F" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Samsung" FontWeight="Bold"/>
                        <TextBlock Text="87" FontSize="28" Foreground="{StaticResource PrimaryColor}"/>
                    </StackPanel>
                </Border>
                <Border Width="180" Margin="0,0,15,15" Padding="15" Background="#1F1F1F" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Xiaomi" FontWeight="Bold"/>
                        <TextBlock Text="64" FontSize="28" Foreground="{StaticResource PrimaryColor}"/>
                    </StackPanel>
                </Border>
                <Border Width="180" Margin="0,0,15,15" Padding="15" Background="#1F1F1F" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Motorola" FontWeight="Bold"/>
                        <TextBlock Text="41" FontSize="28" Foreground="{StaticResource PrimaryColor}"/>
                    </StackPanel>
                </Border>
            </WrapPanel>
        </StackPanel>
    </Grid>
</Page>